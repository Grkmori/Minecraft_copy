namespace BlueBird.World.Parameters {
	public enum EntityType_enu {
		InvisibleData, // No graphics
		Topography,
		Vegetation,
		Structure,
		Animal,
		Monster,
		Character,
		Climate, // Weather phenomenon and climate effects
		Count // Count is just a nice trick to get the enum length
	}
}